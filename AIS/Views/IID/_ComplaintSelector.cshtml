@* Reusable Complaint dropdown *@
<div class="row mb-3">
  <div class="col-md-6">
    <label class="form-label fw-bold">Select Complaint</label>
    <select id="iidComplaintSelect" class="form-select">
      <option value="">-- Select Complaint --</option>
    </select>
    <small class="text-muted">List shows Complaint ID + Nature + Status</small>
  </div>
</div>

<div id="iidAlertHost"></div>

<input type="hidden" id="selectedComplaintId" value="" />
